import 'package:flutter/cupertino.dart';

class ProfileView extends StatelessWidget {
  const ProfileView({super.key, String? userId});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
